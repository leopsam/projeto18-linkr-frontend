import { Bars } from "react-loader-spinner";

const ButtonLoading = (
    <Bars
      height="30"
      width="80"
      radius="9"
      color="white"
      ariaLabel="bars-loading"
      wrapperStyle={{}}
      wrapperClassName=""
      visible={true}
    />
  );

  export default ButtonLoading;